server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: orderly
  datasource:
    url: jdbc:postgresql://localhost:5432/orderly
    username: orderly
    password: orderly
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

app:
  jwt:
    secret: ${JWT_SECRET:dGhpcyBpcyBhIHNlY3JldCBrZXkgZm9yIGRldmVsb3BtZW50IG9ubHk=}
    expiration-ms: 86400000
